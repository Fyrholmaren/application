/* var text = "We Are a Design Studio From Columbus, Ohio";

console.log(text.length);

for (var i = 0; i < text.length; i++){
  $('.container').append('<span>' + text.length[i] + '</span>');
} */